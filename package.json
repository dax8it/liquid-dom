{
  "name": "liquid-glass-web",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --parallel --filter liquid-glass-dom --filter editor dev",
    "dev:minimal": "pnpm --parallel --filter liquid-glass-dom --filter minimal dev"
  }
}
